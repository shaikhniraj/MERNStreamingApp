# values-secrets.yaml
# NEVER commit this file — it holds real credentials, not placeholders.
# Confirm it's in .gitignore before going any further.
# Rename values-secrets.yaml-template to values-secrets.yaml and fill in your real credentials before using.

secrets:
  jwtSecret: "<a long random string>"
  awsAccessKeyId: "<your AWS access key ID>"
  awsSecretAccessKey: "<your AWS secret access key>"